{
  "dependencies": {
    "currently": "^0.0.8"
  },
  "name": "taskquest",
  "version": "1.0.0",
  "devDependencies": {
    "concurrently": "^8.2.1"
  },
  "scripts": {
    "server-client": "cd ./front && npm run start",
    "server-back": "cd ./back && node ./index.js",
    "start": "concurrently \"npm run server-client\" \"npm run server-back\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
